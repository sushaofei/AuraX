# 开发阶段校验清单

## 工作台改造代码交付（2026-09-03，[Issue #12](https://github.com/sushaofei/AuraX/issues/12)）

- [x] 对话和任务使用固定可拖拽执行轨迹，Session 状态固定在顶部，正文和轨迹独立滚动。
- [x] 任务页单轮提交、禁止追问、完成后自动结束 Session；关闭窗口不取消远端任务。
- [x] MCP 管理按类型和一级前缀分组，展示安全标签、Schema，支持只读输入测试及实际输出/期望子集校验。
- [x] 旧版 capabilities 404 回退 tools；目录失败使用中文诊断，不伪装为空目录。
- [x] Skill 列表/详情/治理和暖纸色主题改造一并交付，保留已签名包发布契约，不处理 AuraSkill 源码目录。
- [x] TypeScript、构建、SDK 51 项、Playwright 20 项无重试通过；ESLint 无错误，保留 5 项既有 hooks 警告。
- [x] 本轮相关源码、SDK、测试和文档审查完成；与已独立提交的历史修复 #11 分开提交。
- [ ] 发布后的真实环境验收继续在 #10、#11、#12 跟踪；提交推送不代表部署完成。

## 暖纸色与紫色强调主题（2026-09-03）

- [x] 根据用户提供的 Discovery Loop 参考图适配全局主题，不复制品牌素材或引入外部字体。
- [x] 对话、任务、历史、MCP、Skill、配置统一暖纸色与墨黑；导航浅色，轨迹暖炭黑；保留既有布局与交互。
- [x] 统一语义颜色变量，减少大面积渐变、冷灰色和阴影；主按钮使用紫底白字。
- [x] 实际渲染的导航、辅助文字、主按钮、轨迹正文对比度至少 4.5:1；键盘焦点和减少动态效果检查通过。
- [x] 生产构建、17 项 Playwright 回归通过（2 workers）；ESLint 无错误，保留 ChatView 既有 5 项警告。
- [x] 对话、MCP、Skill 本地截图已检查；设计变量与应用规则记录于 `docs/视觉风格与配色.md`。
- [x] 无业务 API、数据迁移、身份或任务生命周期变更。
- [ ] 发布新版 AuraX 后真实环境验收；尚未部署，不需发布 AuraClaw。
- [x] Git 交付范围已统一审查，纳入上方工作台代码交付阶段；远端凭据记录在 #12。

## Skill 信息层级与视觉改造（2026-09-03）

- [x] 沿用 MCP 的列表 → 详情结构；技能按发布者分组，提供搜索与安装状态筛选。
- [x] 技能详情分为说明、能力依赖、版本治理，桌面侧栏固定显示安装状态、版本约束与管理动作。
- [x] 发布入口按需展开；展示文件数、上传中、成功与错误状态，沿用已签名包上传契约。
- [x] 发布者列表、注册、公钥与状态治理分层；准入审计展示指标、中文结果、可展开诊断与分页。
- [x] 状态使用中文标签；保留原始状态码和包摘要供诊断。只读展示依赖，不发起能力调用。
- [x] 保留安装修订版本、卸载模式、撤销确认、保全检查；无后端或数据迁移，无新增凭证存储。
- [x] 桌面截图与 760px 窄屏检查通过；窄屏摘要移至正文前，不发生横向溢出。
- [x] TypeScript、生产构建、16 项 Playwright 回归通过；ESLint 无错误（保留 ChatView 既有 5 项 hooks 警告）。
- [ ] 发布新版 AuraX 后进行真实环境验收；本轮尚未部署。
- [x] 与本轮工作台改造统一审查并交付，远端凭据记录在 #12。

本清单是 AuraX v1（[Issue #1](https://github.com/sushaofei/AuraX/issues/1)）的完成门禁。阶段只有在功能、架构、质量和交付项全部通过后，才允许标记完成、提交 Git 并推送远端。

## 通用阶段门禁

- [x] 阶段范围与不包含范围已明确（定时、完整登录、Skill 发布不在 v1）。
- [x] 功能列表逐项完成，并有对应测试或可复现验证证据。
- [x] Session 事实在 AuraClaw；AuraX 只发命令、查投影、订 SSE。
- [x] 身份头、幂等键、`credential_ref`、关窗不 cancel 已检查。
- [x] 不涉及 AuraX 自有数据库 Schema；本机仅缓存连接 / 草稿 / last session（可丢）。
- [x] `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm e2e` 全部通过。
- [x] README、AGENTS、签名说明已同步。
- [x] Git 暂存范围已审查，不包含 `.env`、证书、缓存或 Secret。

## Phase 0 — 仓库与壳

- [x] pnpm workspace、TS strict、eslint、vitest。
- [x] Tauri 2 macOS 空窗；展示名 AuraX，Bundle ID `app.aurax.desktop`。
- [x] `claw-sdk` 注入 `MOCK_IDENTITY`（`platform` / `local-org` / `local-user`）与幂等键。
- [x] 连接设置只改 AuraClaw base URL，无账号切换。

## Phase 1 — 实时对话

- [x] 创建任务（`source=chat`）、SSE、transcript / result 回填、追问、取消、关闭。
- [x] HITL 卡片走审批命令；`waiting_for_human` 不能当普通追问。
- [x] 断线用 `Last-Event-ID` 重连；流结束不是任务失败。
- [x] 关闭窗口不会调用 cancel。

## Phase 2 — Session 历史

- [x] 历史走 `GET /v1/tasks?kind=chat`；本机索引仅缓存。
- [x] 打开即恢复 transcript；paused / retry_wait 可 resume（非人审）。

## Phase 3 — MCP 管理

- [x] 登记 / 探测 / 启停 / 对账 / 退役 / Catalog tools。
- [x] Secret 只填 `credential_ref`；loopback 相对 Credential Proxy。
- [x] 不直连 AuraMCP，不调用 `/internal/v1/*`。

## Phase 4 — Skill 管理

- [x] 目录、详情、`SKILL.md`、租户启停。
- [x] 对话页 Skill 选择器（租户目录，下一轮 Run 生效）。
- [x] 无发布入口。

## Phase 5 — 定时任务

- [x] 不在 v1 实现。无本机 cron / 菜单栏调度 / 漏跑补跑。等 AuraAPI Timer。

## Phase 6 — 打包与门禁

- [x] 签名/公证准备：ad-hoc `signingIdentity: "-"`、`Entitlements.plist`、macOS 13+；真实证书步骤见 `docs/macos-signing.md`。
- [x] 关键路径 Playwright：工作台导航、模拟身份、MCP 无明文 Secret、关窗路径不 cancel、不打内部 API。
- [x] 本阶段校验清单已记录。
- [x] GitHub Actions：`typecheck` / `lint` / `test` / `e2e`。

## Phase 7 — 产品级对话执行轨迹（[Issue #2](https://github.com/sushaofei/AuraX/issues/2)）

- [x] 产品方案明确 transcript/activity/result/operations timeline 边界。
- [x] SDK 支持 Activity 契约、增量游标和未知节点前向兼容。
- [x] 对话页右侧面板支持展开/收回、Run 分组、筛选和节点详情。
- [x] 窄窗口使用可关闭抽屉，不影响主对话可用性。
- [x] SSE 只触发 Query 刷新；刷新/重连从 Activity API 权威恢复。
- [x] localStorage 只保存展示偏好，不保存 Activity 详情。
- [x] loading、empty、error、unsupported、running、waiting、failed、cancelled 状态完整。
- [x] SDK tests、typecheck、lint、生产构建与 Playwright 关键路径通过。
- [x] README、产品方案和 AuraClaw API 文档同步。
- [x] 本阶段随 `03c18c8` intentional commit 完成并 push。

## Phase 8 — Skill 全生命周期管理（[Issue #4](https://github.com/sushaofei/AuraX/issues/4)）

- [x] SDK 拆分 Catalog、Installation、Publication、Package、Source、Publisher/Key 和 Admission DTO。
- [x] 修复旧启停契约：使用 Installation revision、reason code 与正确响应类型。
- [x] Catalog 页面展示服务端 availability、版本治理、SKILL.md 与依赖摘要。
- [x] Installation 支持 enable、disable、draining uninstall 与显式 force。
- [x] Source 支持创建、编辑、同步健康、显式同步和退役；Secret 仍仅使用 credential_ref。
- [x] Publisher 支持注册、公钥轮换/撤销和 suspend/resume/revoke；私钥不进入 AuraX。
- [x] 已签名小包使用 direct files，大包把 canonical archive 代理给 AuraClaw，由后端选择 single/multipart。
- [x] Publication revoke/restore、Package purge 与 Admission 指标/筛选/游标翻页已进入管理界面。
- [x] SDK unit/contract tests、TypeScript typecheck 与生产构建通过。
- [x] Playwright 覆盖 Skill 管理入口、禁止内部 API/直连 AuraMCP，并与既有关键路径共同通过。
- [x] 全仓 ESLint 通过；既有 ChatView hooks dependency 警告已记录但不阻断门禁。
- [x] 测试 OBS 完成 AuraClaw single/multipart 代理上传、服务端 ETag/finalize 与对象清理；AuraX 不直连对象存储。
- [x] 与 AuraClaw #59 的 Catalog/同源 API 冒烟、跨仓完整门禁、intentional commit 和 push 完成。

## Phase 9 — Skill 激活失败可见性（[AuraClaw Issue #79](https://github.com/sushaofei/AuraClaw/issues/79)）

- [x] 对话页按当前 Run 独立显示安全错误码与摘要，明确 transcript 仅为会话历史。
- [x] MCP 页面显示 Catalog publication 状态、generation、stale 与最后同步错误，不以聚合 Runtime 状态替代目录健康状态。
- [x] `reconcile` 操作文案改为“同步目录”，并保留目录为空时的操作提示。
- [x] SDK 47 tests、TypeScript、lint 和 Playwright 11 tests 通过。

## Phase 10 — 测试身份配置

- [x] 配置页支持填写必填的租户 ID、部门 ID 和用户 ID，并保留既有 AuraClaw uplink 配置。
- [x] 测试身份仅保存在本机 localStorage，不包含 Token、Secret 或 `credential_ref` 明文。
- [x] 保存后重建 `ClawClient`，普通请求、上传和 SSE 统一注入配置后的身份 Header。
- [x] 切换测试身份时清除当前会话选择，避免用新租户继续读取旧租户 Session。
- [x] SDK 单测与 Playwright 覆盖默认值、自定义 Header、持久化和实际请求注入。

## 历史列表修复 — 2026-09-03（[Issue #11](https://github.com/sushaofei/AuraX/issues/11)，未发布）

- [x] 完成、失败、取消按最近一轮 `run_status` 筛选，Session 关闭状态单独展示。
- [x] 本机类型标记缺失的历史归入“未分类”，不丢弃、不推断为单轮任务；明确当前接口不能还原页面来源。
- [x] 历史缓存键包含 uplink、租户、部门和用户，避免切换身份后串用缓存。
- [x] 增加游标分页、刷新、加载状态和清除筛选；错误不伪装为空列表，说明筛选仅作用于已加载记录。
- [x] TypeScript、生产构建、SDK 51 项测试、Playwright 20 项测试通过（其中 3 项新增历史回归）。
- [x] ESLint 无错误；保留既有 ChatView 的 5 项 hooks dependency 警告，不在本次修复中扩展修改。
- [x] 无后端数据迁移、历史删除或租户身份变更。
- [ ] 发布 AuraX 测试环境并以真实身份验证历史筛选与分页。
- [x] 本次提交只包含历史页、独立样式、筛选辅助函数、回归测试和本节记录；保留其他未提交 UI 改动。
- [x] 独立暂存快照通过类型检查、构建、SDK 47 项测试及 ESLint（5 项既有警告）；页面回归 14 项首次通过，既有同步调用测试 1 项重试后通过，未把该不稳定项计为首次通过。
- [x] 新增 3 项历史回归在独立快照中无重试复验通过。
- [ ] 远端同步与发布后验收由 Issue #11 跟踪。

## 三档审批选择器（Issue #13，2026-09-03）

- [x] 对话默认请求批准；任务 sync / async+wait 默认完全访问权限，显式三档选择均由 SDK 传递。
- [x] 共用选择器、键盘导航、橙色全权限、窄窗口和历史有效模式恢复；旧后端明确显示不支持。
- [x] 草稿模式按环境/身份/Session 隔离且不持久化为全局偏好；运行中/待审批禁止更改。
- [x] 下一 Run 模式与同一幂等键穿过冲突重试，409 needs_human/needs_resume 保留可恢复 Session。
- [x] 客户端无自动代点批准，自动审核结果通过现有执行轨迹与人工审批卡片展示。
- [x] SDK 54 tests、typecheck、lint（仅 5 条既有 ChatView warnings）通过；浏览器全量 22 项及修正后 2 项通过。
- [x] 最终 typecheck、54 SDK tests、lint 和浏览器复验通过；提交范围排除用户既有 styles.css/视觉文档修改。

提交/推送状态以 Git 和 Issue 交付记录为准。

## Skill 升级与自动清理（AuraClaw #94，2026-09-03）

- [x] 发布前读取当前 publication/installation revision，SDK 文件与 Artifact 发布均携带 CAS。
- [x] 当前 Skill 详情区分等待旧调用、删除中、阻塞重试、完成；只有后端 completed 才显示已删除。
- [x] 仅展示当前版本；刷新同时更新目录、详情和管理状态，无本地定时器。
- [x] SDK 55 tests、desktop typecheck、ESLint（仅既有 5 项 ChatView warnings）通过。
- [x] Skill 浏览器测试通过并检查完成状态截图，覆盖阶段切换及隐藏旧版本。
- [x] 无后端凭据/私钥处理，无数据迁移；发布依赖 AuraClaw 0063 与新内部/管理契约。
- [x] 提交排除既有 styles.css 和视觉文档修改。
- [ ] 测试环境部署、真实签名 Skill 升级验收另行记录。
